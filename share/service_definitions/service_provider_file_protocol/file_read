services:
  - name: service_provider_file_protocol/file_read
    request:
      password: { type: uint32_t, default: 0 }
      file_name: { type: string }
    response:
      file_data: { type: uint8_t, array: true }
      error_message: { type: string, default: "no error" }

